sudo: false

language: node_js

node_js:
  - "0.10"
  - "0.12"
  - "4"
  - "6"
  - "8"

matrix:
  fast_finish: true

script: if [ $(echo "${TRAVIS_NODE_VERSION}" | cut -d'.' -f1) -ge 4 ]; then
          npm run cover;
        else
          npm test;
        fi

